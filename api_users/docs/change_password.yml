Change password for the user
---
parameters:
  - name: login
    in: path
    type: string
    required: true
definitions:
  Object:
    type: object
    properties:
      password:
        type: string
responses:
  200:
    description: Returns true if the registration is successful
    schema:
      properties:
        registration:
          type: boolean
          update: true if the password was correctly updated
          default: true
        reason:
          type: string
          description: if the password update isn't successful, a short description of why
          default: 