Auth user
---
parameters:
  - in: header
    name: Authorization
    type: Basic
    required: true

responses:
  200:
    description: Returns true if the user auth
    schema:
      properties:
        auth:
          type: boolean
          description: true if the user auth
          default: true
        reason:
          type: string
          description: if the auth isn't successful, a short description of why