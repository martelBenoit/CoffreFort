version: '3'
services:

    flask:
        build: .
        command: python -u app.py
        ports:
            - "5000:5000"
        volumes:
            - .:/app
        links:
            - mongodb

    mongodb:
        image: mongo:4.0.8